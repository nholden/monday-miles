language: ruby
rvm:
  - 2.5.0
install:
  - bundle install
  - yarn install
before_script:
  - psql -c 'create database travis_ci_test;' -U postgres
  - cp config/database.yml.travis config/database.yml
script:
  - bundle exec rspec
services:
  - postgresql
notifications:
  email: false
